export enum MaterialType{
	MESH_PHONE_MATERIAL = 'MESH_PHONE_MATERIAL',
	MESH_STANDARD_MATERIAL = 'MESH_STANDARD_MATERIAL',
}

const createScene = (type?: keyof keyof)=>{

}

export default createScene;
